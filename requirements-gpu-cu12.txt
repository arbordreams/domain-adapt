# GPU/CUDA 12.x pinned requirements.
# Use only if you are on a CUDA 12.0+ system. Matches RunPod images with CUDA 12.8.
# IMPORTANT: Prefer the container's preinstalled CUDA libraries. This set pins to compatible torch/vision.
--extra-index-url https://download.pytorch.org/whl/cu121
# Torch family (select the right wheel per your CUDA runtime; for CUDA 12.8, these are compatible)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Base libs mirror requirements.txt (do not duplicate torch here)
transformers==4.45.2
datasets==2.21.0
huggingface_hub==0.26.2
hf-transfer>=0.1.5
safetensors==0.4.5
sentencepiece==0.2.0
accelerate==0.34.2
pyyaml>=6.0.1
fsspec>=2024.6.1
aiohttp>=3.9.5
gensim==4.3.3
numpy==1.26.4
ujson>=5.9.0
psutil>=5.9.8
rich>=13.7.1
orjson>=3.10.3
xxhash>=3.4.1

